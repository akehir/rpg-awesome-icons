version: "3.9"

services:
    tfc:
        container_name: rpg-awesome-icons-example-app
        image: rpg-awesome-icons-example-app
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        ports:
            - "8080:8811"
